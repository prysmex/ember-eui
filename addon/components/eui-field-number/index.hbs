{{#let
  (class-names
    (if @icon "euiFieldNumber--withIcon")
    (if @fullWidth "euiFieldNumber--fullWidth")
    (if @compressed "euiFieldNumber--compressed")
    (if (or (has-block "prepend") (has-block "append")) "euiFieldNumber--inGroup")
    (if @isLoading "euiFieldNumber--isLoading")
    "euiFieldNumber"
  ) as |classes|
}}
  {{#if @controlOnly}}
    <input
      id={{@id}}
      class={{classes}}
      value={{@value}}
      type="number"
      ...attributes
      {{validatable-control @isInvalid}}
    />
  {{else}}
    <EuiFormControlLayout
      @icon={{@icon}}
      @fullWidth={{@fullWidth}}
      @isLoading={{@isLoading}}
      @compressed={{@compressed}}
      @readOnly={{@readOnly}}
      @append={{has-block "append"}}
      @prepend={{has-block "prepend"}}
      @inputId={{@id}}
    >
      <:prepend as |api|>
        {{yield api to="prepend"}}
      </:prepend>
      <:field>
        <input
          id={{@id}}
          class={{classes}}
          value={{@value}}
          type="number"
          ...attributes
          {{validatable-control @isInvalid}}
        />
      </:field>
      <:append as |api|>
        {{yield api to="append"}}
      </:append>
    </EuiFormControlLayout>
  {{/if}}
{{/let}}