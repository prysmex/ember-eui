{{! TODO: Add Beta Badge Component for Beta Label props}}
<EuiPanel
  @hasShadow={{@hasShadow}}
  @paddingSize={{@paddingSize}}
  @grow={{@grow}}
  class={{class-names
    componentName="EuiPageContent"
    verticalPosition=@verticalPosition
    horizontalPosition=@horizontalPosition
  }}
  ...attributes
>
  {{yield
    (hash
      Header=(component
        "eui-page-content-header"
      )
      Body=(component
        "eui-page-content-body"
      )
    )
  }}
</EuiPanel>