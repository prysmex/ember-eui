import htmlIdGenerator from 'dummy/utils/html-id-generator';
import { module, test } from 'qunit';

module('Unit | Utility | html-id-generator', function() {

  // TODO: Replace this with your real tests.
  test('it works', function(assert) {
    let result = htmlIdGenerator();
    assert.ok(result);
  });
});
